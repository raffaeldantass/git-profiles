import React from 'react';

const Footer = ({className}) => {
  return (
    <footer className={ className }> 
      <p className= "footer__text"> Feito por Rafael Dantas </p>
    </footer>
  )
}

export default Footer;